const Router = require("express");
const router = new Router();

router.post("/"); // create a brand
router.get("/"); //  get a brand
// router.delete("/"); //  delete a brand

module.exports = router;
